require "spec_helper"

RSpec.describe ShowCode do
  it "has a version number" do
    expect(ShowCode::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
